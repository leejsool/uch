# UniverCityHunter 인수인계

취미 프로젝트. 대학가에 게이트가 열리고 학생들이 전공별 능력으로 맞서는 로그라이크.
탐색은 턴제, 전투는 2D 탑뷰 실시간 자동전투. 상업 목적 아님, 지인 공유용.

작성일 2026-09-07 · 이 문서가 현재 사양의 기준입니다.

---

## 0. 먼저 읽을 것 — 파일이 갈라져 있습니다

같은 게임의 사본이 **두 갈래** 존재합니다.

| | 있는 것 | 없는 것 |
|---|---|---|
| **이 폴더** (기준) | 스킬 개편, 길찾기 수정, 몬스터 그림 4장, 아이템 그림 13장, 초상 6장, 아이템 도감, 버튼 접기, 학과사무실 | — |
| `ucw/dist/` 계통 | 학과사무실(먼저 만든 판) | 위의 나머지 전부 |

**이 폴더가 최신입니다.** 다른 사본을 배포하면 작업이 되감깁니다.
로컬로 옮기신 뒤에는 이 폴더 하나만 남기고 나머지는 지우시는 편이 안전합니다.

---

## 1. 파일 구조

```
index.html            게임 전체 (200KB). 외부 라이브러리 없음, 캔버스 2D만 사용
build-single.py       art/ 의 그림을 base64 로 박아 단일 파일 배포본을 만듭니다
읽어보기.md            그림을 추가하는 방법
전투장비배치.html      손장비 아이콘 배치 (BATTLE_RIG)
전투지형관리.html      장소별 벽·감속 지대 + 대원·적 스폰 자리 (ROOMS·DOOR)
캐릭터이동.html        index 의 전투 코드를 그대로 불러와 조이스틱으로 이동 확인
art/
  campus.webp title.webp
  bld/    main med arts            건물 배경
  intro/  gate                     인트로
  face/   lee · m-01~m-18 f-01~f-14 (+ m-vit-1 m-spd-1 f-spd-1) · m-anon f-anon   대원 초상
  foe/    worm hopper hopperFly mantis                몬스터
  item/   ladle lid tuna pen book jacket doorpanel boots sneakers slippers snack track spray
  npc/    office shop
  fac/    (비어 있음)
```

실제 작업은 `index.html` 의 `<script>` 안 JS(약 175KB)만 손댑니다. HTML/CSS 헤더는 거의 고정.
브라우저에서 바로 열립니다. APK 로 만들려면 Capacitor 등으로 감싸면 됩니다.

### 배포

```bash
cp index.html univercityhunter.html
python3 build-single.py          # → univercityhunter-single.html (약 2.4MB)
```

`build-single.py` 는 소스에서 `"경로/이름.webp"` 꼴 문자열을 찾아 `art/` 밑에서 자동으로
집어넣습니다. 새 그림을 표에 한 줄 적으면 별도 설정 없이 딸려 들어갑니다.

GitHub Pages: `leejsool/uch` → https://leejsool.github.io/uch/

### 작업 시 주의

파일을 고치는 스크립트에서 **읽기보다 쓰기가 먼저 실행되면 소스가 통째로 날아갑니다.**
한 번 겪었음. 반드시 읽기 → 변환 → 쓰기 순서로.

### 검증 방법

```bash
# 문법 검사 — <script> 만 뽑아 node --check
node -e "const fs=require('fs'),s=fs.readFileSync('index.html','utf8');\
fs.writeFileSync('chk.js',s.match(/<script>([\s\S]*?)<\/script>/)[1])"
node --check chk.js
```

동작 검증은 **로컬 서버를 띄우고 브라우저에서 실제로 돌려 보는 방식**이 가장 확실합니다.
`file://` 로 열면 `fetch` 가 막혀 도구들의 "index.html 불러오기" 가 안 됩니다.

```bash
# C:\uch 를 http 로 내보내는 20줄짜리 서버 (도구 3종의 불러오기도 이때 동작)
node serve.js      # → http://localhost:8788/index.html
```

jsdom 으로도 되지만(`npm i jsdom`, 전투 그림은 `npm i canvas`), 브라우저에서 직접
`newGame(0)` → `startBattle(...)` → `tick(1/60)` 을 불러 보는 편이 빠릅니다.

### 그림 자르기 (webp)

이 PC 에는 cwebp·ffmpeg·magick 이 없고 .NET 에도 webp 인코더가 없습니다.
**브라우저 캔버스가 유일한 webp 인코더**입니다 — 서버로 원본을 물려주고,
`canvas.toBlob(…, "image/webp", 0.86)` 으로 구운 뒤 서버의 저장 엔드포인트로 되돌려 씁니다.
초상 32장을 이 방식으로 만들었습니다.

**밸런스 시뮬레이션은 요청할 때만.** 매번 승률을 재면 시간과 비용이 큽니다.

---

## 2. 개발용 스위치

코드 맨 위:

```js
const DEV_SHOW_UNLOCK = true;
```

`true` 일 때만 노출되는 것:
- 잠긴 시설의 해금 조건과 진행도
- 장소 선택 화면에서 아직 못 얻어 본 아이템의 진짜 이름 (평소엔 `??`)
- 본진 상단의 분홍색 `DEV` 배지

**배포 전 `false` 로 바꿀 것.**

---

## 3. 전체 흐름

```
인트로 → 스탯 배분 → 게이트 진입 → 힘의 각성 → 인스턴트 스킬 선택
  → 튜토리얼(음식점, 애벌레 2) → 정산 → 귀환
  → 본진 안내 → 캠퍼스
       ├ 본관    · 행정실 / 창고 겸 학생회 매점 / 입학처
       ├ 대운동장 · 학생회관
       ├ 예체능  · 게임장 / 학과사무실(태권도학과)
       ├ 의학대학 · 응급의학 실습실 / 학과사무실(간호학과) / 메모리얼 홀
       ├ ROTC   · 학과사무실(군사학)
       ├ 이공대학 · 실습동 (미구현)
       ├ 인문사회 · 시설 없음 (영구 잠김)
       ├ 사범대학 · 시설 없음 (영구 잠김)
       └ 정문 게이트 → 출동 확인 → 힘의 각성 → [매일] 스킬 선택 → 장소 선택 → 전투
                      → 정산 → 전리품 정리 → 레벨업 → 다음날 … → 귀환
```

전멸하면 게임 오버. 가진 것 전부 소실.

---

## 4. 캐릭터

### 스탯 (기본 전부 10)

| 스탯 | 키 | 효과 | 개방 |
|---|---|---|---|
| 체력 | vit | 최대 체력 = 20 + 체력×6 + 장비 | Lv1 |
| 힘 | str | 공격력 = 2 + 힘×0.6 + 장비 | Lv1 |
| 속도 | spd | 이동 = (14 + 속도×3) × 장비배율 · **회피 = 속도×0.3%** | Lv1 |
| 순발력 | agi | 공속 = (0.5 + 순발력×0.05) × 장비배율 | Lv1 |
| 운 | luk | 치명타 = 운×0.5% | Lv2 |
| 전공활용 | maj | 스킬 위력 (**미구현**) | Lv3 |

회피는 장비·스킬 포함 **최대 60%**, 치명타는 **최대 80%** 로 잘립니다.
지원자 생성은 총합 60 근처를 유지한 채 스탯끼리 밀고 당겨 개성을 만듭니다(±1 편차).

### 레벨

`EXP_TABLE = [0, 90, 700, 1500, 2800, 4600]`, 최대 Lv5.
레벨업당 스탯 포인트 1점. **Lv2 는 사마귀(3일차)를 만나기 직전에 오도록 90 으로 낮춰 두었습니다** —
학과사무실 해금이 Lv2 이기 때문입니다.
성장의 주축은 레벨이 아니라 게이트 안의 인스턴트 스킬.

### 초상

`FACE_ART` 에 **성별 두 줄**로 등록합니다. 스탯은 보지 않습니다(옛 "성별-스탯" 칸 방식은 폐지).

```js
const FACE_ART={
  m:["face/m-01.webp", …, "face/m-18.webp", "face/m-vit-1.webp","face/m-spd-1.webp"],  // 20장
  f:["face/f-01.webp", …, "face/f-14.webp", "face/f-spd-1.webp"],                      // 15장
  anon:{m:"face/m-anon.webp", f:"face/f-anon.webp"},
};
```

- 지원자가 만들어질 때 **성별 풀에서 무작위로** 하나 받습니다 (`pickFace`)
- **한 번 나간 초상은 다시 안 나옵니다** (`G.facesUsed`). 줄 것이 없으면 어나니머스
- 어나니머스는 여럿이 써도 되고 `facesUsed` 에 쌓이지 않습니다
- 기록 시점은 **명단에 실제로 합류할 때**(`claimFace`). 지원자를 새로고침해도 낭비되지 않습니다
- 지원자는 `rollApplicants(n)` 으로 한 명씩 명단에 얹어 가며 뽑습니다.
  한꺼번에 만들면 서로의 초상을 못 봐서 같은 얼굴이 겹칩니다

현재 남 20장 · 여 15장. 그보다 많이 뽑으면 그때부터 어나니머스로 갑니다.

초상은 **320×320 webp**, 상체가 들어가는 구도(얼굴만 바짝 자르지 않습니다).
카톡으로 받은 2×2 시트(1254×1254, 가운데 흰 여백 세로 619~634 · 가로 618~631)를
사분면 통째로(618×618) 잘라 쓴 것입니다. 자르는 방법은 아래 "그림 자르기" 참고.

---

## 5. 전공과 학과사무실

`MAJOR_LV = 2`. 파티 최고 레벨이 2가 되면 학과사무실이 열립니다.
**전공은 한 번 정하면 못 바꿉니다.**

| 건물 | 시설 | 전공 | 해금 조건 |
|---|---|---|---|
| 예체능대학 | deptArts | 태권도학과 (tkd) | Lv2 |
| 의학대학 | deptMed | 간호학과 (nurse) | Lv2 |
| ROTC | deptRotc | **군사학 (mil)** | **총기를 들고 살아 귀환** + Lv2 |

`G.gunHome` 은 귀환 정산에서 살아 돌아온 대원의 장비 슬롯과 배낭을 뒤져
탄약을 쓰는 물건이 하나라도 있으면 섭니다. 전멸하면 안 섭니다.

`GUN_MAJORS = ["soldier","police","mil"]` — 이 전공만 총을 듭니다.
전공이 안 맞으면 착용 버튼을 눌렀을 때 화면 아래에 `전공이 적합하지 않습니다` 알림이 뜹니다.

### 아직 못 얻는 전공 9종

`MAJORS` 에는 남아 있지만 배정해 줄 학과사무실이 없습니다.
군인 · 경찰대학생 · 화학과 · 철학과 · 체육교육과 · 의예과 · 컴퓨터공학과 · 무용과 · 건축학과 · 문헌정보학과
(의예과는 「응급 처치」를 간호학과로 넘겨서 스킬이 없습니다)

**도서관은 폐기했습니다.** 예전에는 여기서 10종을 자유롭게 골랐습니다.

---

## 6. 전투 시스템

### 높이 레벨 (비트마스크)

`L1=바닥 / L2=허리 / L3=머리 위`. **겹치는 층이 하나라도 있으면 서로 막힙니다.**
유닛끼리도 같은 규칙이라 같은 층을 쓰면 밀어냅니다.

| 대상 | 점유 층 |
|---|---|
| 사람 | L1+L2 |
| 애벌레 | L1 |
| 메뚜기 | L1+L2 |
| 사마귀 · Blood Eyes · 좀비 | L1+L2+L3 |
| 거미 | L2+L3 (다리가 길어 낮은 지형을 넘어감) |
| 식탁·화단·소파 | L1 |
| 의자·칸막이 | L2 |
| 주방·쓰레기·차·계단 | L1+L2 |
| 냉장고·건물·기둥 | L1+L2+L3 |

### 길찾기 — **몸집별로 나뉩니다**

방을 25×25 격자(`GN=25, CELL=4`)로 나눠 목표 칸에서 BFS 거리 지도를 만듭니다.

**통행 지도는 반지름 3 / 5 / 7 세 단계로 따로 만듭니다** (`rBucket`).
전부 반지름 3으로 계산하면, 사람이 겨우 지나는 틈을 지도가 "뚫려 있다"고 알려 주고
덩치 큰 보스가 그리로 들어갔다가 끼어 제자리걸음을 합니다. 이걸로 좀비 보스의 끼임이
전투당 4.8초에서 0.1초로 줄었습니다.

`moveTo` 는 **제자리걸음이 0.2초를 넘으면 직선을 포기하고 지도를 따릅니다.**
ㄱ자 모서리에서는 직선이 "거의" 뚫린 것처럼 보여 계속 벽을 밀게 되는데, 그때 넘어옵니다.
그래도 안 풀리면 0.45초에 옆걸음, 1.5초에 방향 재설정.
대각선으로 모서리를 스치는 것도 막아 두었습니다.

캐시(`PASS_CACHE`, `FIELD_CACHE`)는 `startBattle()` 에서 초기화. 필드 캐시 상한 240.

### 사거리 규칙 (중요)

적의 유효 사거리 = **자기 사거리 + 상대 반지름**.
이걸 빼먹으면 덩치 큰 적이 밀어내기 거리 때문에 영영 공격을 못 합니다.

반대로 이 규칙 때문에 **몸집 큰 보스는 구조적으로 사거리에서 불리합니다.**
대원 사거리 11이면 Blood Eyes(r 6.6)에 닿는 거리는 17.6, 보스가 대원(r 3.2)에 닿는 거리는 14.2.
전에 「길어진 팔+」(사거리 +4)를 넣었을 때 이 차이 + 보스의 끼임이 겹쳐
Blood Eyes 승률이 0% → 38% 로 튀었습니다. 길찾기를 고쳐서 지금은 0% 로 돌아왔지만,
**사거리를 더 늘리는 스킬·장비를 넣을 때는 이 조합을 반드시 확인할 것.**

### 공격 모션

| 모션 | 무기 | 준비 | 타격 | 후딜 | 특징 |
|---|---|---|---|---|---|
| swing | 국자·배트·문진·소주병·마이크·경찰봉 | 0.30 | 0.10 | 0.22 | **주변 적에게 50% 광역** (`SPLASH_RATE=0.5`, `SPLASH_PAD=2`) |
| thrust | 만년필·커터칼·연탄집게·헤어스프레이 | 0.18 | 0.07 | 0.20 | 뺐다가 앞으로 |
| gun | 권총·산탄총·소총 | 0.13 | 0.05 | 0.17 | 총구 화염 + 예광선 + 반동 |

피해는 반드시 **타격 프레임**에만 들어갑니다.

### 피해 종류

찌르기 · 베기 · 관통 · 화학 · 깨물기 · 폭발 · 타격
방어구의 `frail` 에 종류별 파손 확률을 적어 둡니다. 부순 그 일격은 방어 없이 그대로 들어갑니다.

---

## 7. 적

| 키 | 이름 | 체력 | 공격력 | 공속 | 이동 | 사거리 | 반지름 | 경험치 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| worm | 애벌레 | 44 | 8 | 0.60 | 20 | 6 | 2.8 | 14 | 자벌레처럼 꿈틀 |
| hopper | 메뚜기 | 36 | 12 | 0.80 | 42 | 6 | 2.9 | 22 | 웅크렸다 도약 |
| mantis | 사마귀 | 180 | 16 | 0.65 | 13 | 9 | 5.2 | 90 | 1게이트 3일차 보스 |
| hornet | 장수말벌 | 240 | 24 | 1.10 | 58 | 8 | 4.0 | 150 | 1게이트 5일차 보스, `lvm:FLY` 라 지형을 거의 다 넘음 |
| spider | 거미 | 540 | 44 | 0.80 | 28 | 16 | 5.8 | 380 | 1게이트 10일차 보스, 거미줄 |
| dogzomb | 강아지 좀비 | 300 | 22 | 1.30 | 66 | 6 | 3.6 | 170 | 2게이트 5일차 보스, 네 발로 달림(`CRAWL`), 도망 패턴 |
| crawlzomb | 다리가 부러진 좀비 | 760 | 66 | 0.55 | 8 | 14 | 5.6 | 330 | 2게이트 10일차 보스, 거의 못 걷고 팔이 김 |
| zombie | 좀비 | 900 | 100 | 100 | 15 | 8 | 6.2 | 900 | 2게이트 15일차 보스, 절뚝거림 |
| bloodeyes | Blood Eyes | 560 | 50 | 2.86 | 46 | 11 | 6.6 | 400 | 3게이트 보스 |
| zomb | 좀비(무리) | 170 | 45 | 0.50 | 17 | 6 | 3.4 | 60 | 날짜 배율 없음 |

- 일반 적은 하루마다 체력 ×1.30, 공격력 ×1.20 (복리)
- **보스는 날짜 배율을 타지 않습니다.** 등장 일차에 맞춰 이미 세게 잡아 두었습니다
- 튜토리얼에서는 모든 적 공격력 −1
- 좀비의 공속 100은 "닿으면 즉사" 를 뜻합니다. 사실상 접촉 판정

### 게이트별 보스·잡몹

보스 일정과 잡몹은 **게이트마다 따로**입니다. `GATES` 표 한 곳만 고치면 됩니다.

```js
const GATES=[
  {id:"front", n:"정문 게이트",  days:10, need:null,    pool:["worm","hopper"],
   boss:{3:"mantis", 5:"hornet", 10:"spider"}},
  {id:"back",  n:"후문 게이트",  days:15, need:"front", pool:["zomb"],
   boss:{5:"dogzomb", 10:"crawlzomb", 15:"zombie"}},
  {id:"hall",  n:"강의동 게이트", days:20, need:"back",  pool:["zomb"],
   boss:{20:"bloodeyes"}, wip:true},
];
BOSS_ADDS = {zombie:{key:"zomb", count:5}};
```

- `days` — 그 게이트의 마지막 날. **그날 보스를 잡으면 게이트가 닫히고 캠퍼스로 돌아옵니다**
- `need` — 앞 게이트 id. 그걸 깨야(`G.cleared`) 열립니다. **레벨은 안 봅니다**
- `pool` — 잡몹. 첫 종류가 기본, 이틀째부터 두 번째가 45% 로 섞임
- `boss` — 일차 → 적 key. 그날 전투에는 100% 섞임
- `wip` — 안쪽을 아직 안 만든 게이트. 열려도 못 들어갑니다(🚧)

**1게이트는 곤충·벌레류만, 2게이트는 좀비 계열만** 나옵니다. Blood Eyes 는 3게이트로 보냈습니다.

마지막 날 보스를 아직 못 잡았으면 `nextDay()` 가 날짜를 올리지 않고 그 날에 머뭅니다 —
더 갈 데가 없다는 뜻입니다. 지나치기를 눌러도 같은 날의 장소가 새로 제시됩니다.

### 도망 패턴 (`flee`)

`ENEMIES` 에 `flee:{at, to, rate}` 를 달면 그 적은 **한 판에 한 번** 물러났다 돌아옵니다.

```js
dogzomb:{ …, flee:{at:0.25, to:0.5, rate:0.05}},   // 25% 밑 → 구석 → 초당 5% 회복 → 50% 에 복귀
```

- `at` 체력이 최대치의 이 비율 밑으로 떨어지면 발동 (`f.fled` 로 1회 제한)
- `to` 최대치의 이 비율까지 차면 다시 덤빔
- `rate` 초당 회복량 (최대 체력 대비). 0.05 면 25%→50% 에 5초
- 물러날 자리는 `fleeDen()` — 네 모서리를 각각 `freeSpot` 으로 옮겨 본 뒤,
  살아 있는 대원들의 한가운데에서 **가장 먼 결과**를 고릅니다.
  모서리가 벽 속인 장소(음식점의 주방 등)에서 엉뚱한 데로 밀리지 않게 하려는 것입니다
- **도망 중에는 공격하지 않고, 무적도 아닙니다.** 쫓아가서 회복 중에 죽일 수 있습니다

### 좀비화

`ZOMB_BITE`(좀비·좀비무리·강아지 좀비)에게 죽은 대원은 **그 자리에서 일어나 적이 됩니다.**
원본 최대 체력의 70%, 공격력의 80%. 이름은 "좀비 ○○".
다리가 부러진 좀비는 물지 않고 찢으므로(`SLASH`) 여기서 빠집니다.

`ZOMB_WALK`(좀비·좀비무리·다리가 부러진 좀비)는 절뚝걸음과 좀비 그림을 같이 씁니다.
강아지 좀비는 네 발이라 빠지고 기본 도형으로 그려집니다.

### 적 수 계산

```
base = 2 + floor(일차/2) + 위험도
n    = clamp(round(base × (0.42 + 0.58 × 파티인원/4)), 1, 6)
```

**파티 인원 비례가 핵심.** 이게 없으면 혼자 들어갔을 때 구조적으로 죽습니다.

### 몬스터 그림

`FOE_ART` 에 등록. **빈 칸이면 지금까지처럼 도형으로 그립니다** — 한 마리씩 채워도 안 깨집니다.

```js
const FOE_ART={worm:"foe/worm.webp", hopper:"foe/hopper.webp",
               mantis:"foe/mantis.webp", spider:"", bloodeyes:"", zombie:"", zomb:""};
FOE_ART.hopperFly="foe/hopperFly.webp";   // 떠 있는 동안만 쓰는 그림
```

- **옆모습 한 장, 머리가 오른쪽(→)** 을 보도록. 코드가 `f.face` 에 맞춰 돌립니다
- 투명 배경, 가로 256~320px
- **충돌 반지름 `r` 은 그림 크기와 무관합니다.** 그림만 키우세요.
  `r` 을 건드리면 길찾기와 사거리 계산이 어긋납니다
- `○○Fly` 를 넣으면 그 몬스터가 떠 있는 동안 그 그림을 씁니다.
  **두 장은 같은 배율·같은 위치에 몸이 오도록 맞춰야 합니다.** 어긋나면 뜨는 순간 몸이 튑니다
  (메뚜기 두 장은 눈동자 위치를 기준으로 맞췄습니다)

구현된 움직임:
- **애벌레** — 그림을 세로 20조각으로 썰어, 앞 36%(머리)는 굳히고 뒤만 접습니다.
  `f.surge` 로 길이를, 사인파로 물결을 만듭니다
- **메뚜기** — 웅크리면 앞뒤로 22% 짧아지고, 뜨면 날개 그림으로 바뀌며 20% 커지고 날개가 파닥입니다
- **사마귀** — 평소 좌우로 흔들리고(±0.05rad), `f.strike` 때 앞으로 `r*0.55` 튀어나갑니다
- 피격 번쩍임은 `ctx.filter="brightness(0) invert(1)"`. 구형 WebView 가 무시해도 안 깨집니다

**거미는 몸통만 그리세요.** 다리 여덟 개는 코드가 번갈아 딛는 쪽이 훨씬 자연스럽습니다.
**Blood Eyes** 는 눈 8개가 따로 깜빡이고 윤곽이 일렁이는 게 코드로 돌아갑니다.
그림으로 덮으면 그 효과가 사라지므로, 몸통만 바꾸는 혼합을 검토할 것.

---

## 8. 장비

슬롯 6개: 오른손 / 왼손 / 머리 / 상의 / 하의 / 신발. 배낭 **1인 5칸** (`BAG_MAX=5`).

분류 3종:
- **반출(perm)** 21종 — 살아서 나가야 지킴
- **현장(inst)** 12종 — 게이트를 나가면 사라짐. 장비 11 + 어둠 에너지 결정
  (여기에 강화판 11종이 자동으로 더 실립니다. 아래 "어둠 에너지 결정" 참고)
- **잡화(junk)** 20종 — 행정실에서 판매. 일부는 소모품
- **탄약(ammo)** 3종

### 주요 장비

| 이름 | 슬롯 | 분류 | 효과 | 비고 |
|---|---|---|---|---|
| 국자 | 오른손 | 반출 | 공+4 · 베기 | 튜토리얼 지급 |
| 만년필 | 오른손 | 반출 | 공+4, 공속+30% · 찌르기 | 매점 |
| 헤어스프레이 | 오른손 | 반출 | 공+4, 공속+25%, 사거리+2 · 화학 | 매점 · 화장품가게 |
| 야구 배트 | 오른손 | 반출 | 공+8, 공속−14% · 타격 | 원룸빌라 |
| 벼려진 문진 | 오른손 | 반출 | 공+6 · 타격 | 독서실 |
| 경찰봉 | 오른손 | 반출 | 공+5, 공속+10% · 타격 | 경찰 본부 |
| 깨진 소주병 | 오른손 | 현장 | 공+7 · 베기 | 주점 |
| 커터칼 | 오른손 | 현장 | 공+4, 공속+40% · 찌르기 | 문구점 |
| 냄비뚜껑 | 왼손 | 반출 | 피해−3, 체+6 · 폭발 50% 파손 | 튜토리얼 지급 |
| 전공책 | 왼손 | 반출 | 피해−4 · 베기 5%/폭발 100% 파손 | 매점 |
| 자동차 문짝 | 왼손 | 현장 | 피해−8, 체+10, 이동−22% | 대로 |
| 학과 점퍼 | 상의 | 반출 | 피해−1, 체+8 | 매점 |
| 역무원 방검조끼 | 상의 | 반출 | 피해−4, 체+8 | 지하철역 |
| 방탄조끼 | 상의 | 반출 | 피해−6, 체+10 | 경찰 본부 |
| 트레이닝 바지 | 하의 | 반출 | 회피 5% | 골목길 |
| 안전화 | 신발 | 반출 | 피해−1, 이동+8% | 대로 |
| 운동화 | 신발 | 반출 | 이동+14% | 버스 정류장 |
| 삼선 슬리퍼 | 신발 | 현장 | 회피 5%, 치명 +3% | 원룸빌라 |

### 어둠 에너지 결정 — 현장 장비 강화

게이트 안에서만 도는 강화 자원입니다. **반출품은 대상이 아닙니다.**
어차피 문을 나서면 사라질 물건이니 세게 줘도 본진 경제가 흔들리지 않습니다.

- 전리품으로 나옵니다. 확률 `CRYSTAL_RATE(0.10) + 위험도 × CRYSTAL_RISK(0.05)` —
  위험도 0 에서 10%, 2 에서 20%. 게이트 하나(10~15일)당 평균 한두 개
- 배낭 한 칸을 차지하고, 게이트를 나가면 다른 현장 물건처럼 사라집니다
- 쓰는 곳은 **배낭 줄** — 결정 줄을 누르면 그 사람이 지닌 현장 장비가 버튼으로 뜹니다
  (찬 것 + 배낭에 든 것). 누르면 그 자리에서 바뀝니다. 벗었다 낄 필요가 없습니다
- 강화판은 원본과 **같은 칸·같은 분류(현장)**. 이미 강화된 것은 다시 강화되지 않습니다

강화판은 `DARK_UP` 표에서 자동으로 만들어집니다. **바뀌는 수치만** 한 줄 적으면
`<원본>+` 라는 id·이름으로 `ITEMS` 에 실리고, 설명문도 원본과 같은 어순으로 생성됩니다.

```js
const DARK_UP={ cutter:{atk:7,aspdM:1.55}, … };   // → ITEMS["cutter+"] "커터칼+"
```

| 원본 | 강화판 |
|---|---|
| 깨진 소주병 공+7 | **깨진 소주병+** 공+12 |
| 커터칼 공+4, 공속+40% | **커터칼+** 공+7, 공속+55% |
| 연탄집게 공+6, 공속+10%, 사거리+2 | **연탄집게+** 공+10, 공속+20%, 사거리+3 |
| 마이크 공+4, 공속+15% | **마이크+** 공+7, 공속+30% |
| 쟁반 피해−2, 공속+5% | **쟁반+** 피해−4, 공속+12% |
| 입간판 피해−5, 회피−4% | **입간판+** 피해−9, 회피−3% |
| 자동차 문짝 피해−8, 체+10, 회피−9% | **자동차 문짝+** 피해−13, 체+16, 회피−7% |
| 공사장 헬멧 피해−2 | **공사장 헬멧+** 피해−4 |
| 앞치마 피해−1, 체+4 | **앞치마+** 피해−3, 체+8 |
| 작업복 바지 피해−2 | **작업복 바지+** 피해−4 |
| 삼선 슬리퍼 회피5%, 치명+3% | **삼선 슬리퍼+** 회피9%, 치명+6% |

**그림은 새로 만들지 않습니다.** 원본 그림을 그대로 가져다 검은 아우라만 덧입힙니다 —
목록은 CSS `.ico.dk`(어둡게 + 보라 drop-shadow + `darkpulse` 맥동),
전투는 캔버스에서 같은 그림을 그림자만 켠 채 두 번 덧그립니다(`drawHand`).
전투 손 위치(`BATTLE_RIG`)도 `rigFor` 가 원본 것을 찾아 씁니다. **배치 도구는 손댈 필요 없습니다.**

결정 자체는 아직 그림이 없어 CSS 마름모(`.ico.crys`)로 대신 그립니다.
`art/item/crystal.png` 를 만들어 `ITEM_ART` 에 한 줄 넣으면 그 자리에 들어갑니다.

2단 강화(`++`)를 넣을 때는 `darkId(id, 2)` 가 이미 `커터칼++` 를 만들어 주므로
`DARK_UP` 옆에 2단 표를 하나 더 두고 같은 순환문을 한 번 더 돌리면 됩니다.

### 총기 (군인·경찰대학생·군사학 전용)

| 이름 | 공격력 | 공속 | 사거리 보정 | 탄약 |
|---|---|---|---|---|
| 권총 | +8 | +25% | +9 | 권총 탄약 |
| 진압용 산탄총 | +16 | −30% | +6 | 산탄총 탄약 |
| 소총 | +12 | — | +12 | 소총 탄약 |

근접 기본 사거리가 7이므로 총은 13~19가 됩니다. 전부 관통 피해.
**설명문에는 전공 제한을 적지 않습니다.** 착용을 시도하면 알림으로 알려 줍니다.

### 탄약

- **가방 칸을 차지하지 않고 본부에 쌓입니다** (골드와 같은 방식)
- 종류별로 호환되지 않음
- **해당 총기를 한 번이라도 손에 넣어야**(`G.gunSeen`) 매점에 그 탄약이 뜹니다
- **탄약이 떨어지면 맨손 전환** — 무기 공격력이 빠지고 사거리 7, 휘두르기 모션으로 바뀝니다

### 소모품

참치캔 체력+10 (판매 25G) / 과자 체력+5 (판매 8G)
**배낭에 넣은 뒤 그 사람 배낭 줄에서 `먹는다`** 로 먹입니다. 바닥에서 바로 먹는 길은 없앴습니다.

### 아이템 그림

`ITEM_ART` 에 등록. 64×64 webp. 목록에서 **왼쪽에 44~48px 로 크게** 뜨고 설명이 오른쪽으로 흐릅니다.

### 물건 도감

장소 선택 화면에 그곳에서 나올 수 있는 것이 전부 나열되지만,
**한 번이라도 손에 넣어 본 것만 이름이 보이고 나머지는 `??`** 로 남습니다.

- 기록은 `G.itemSeen` (슬롯별). `markSeen(id)` 를 손에 넣는 길목마다 걸어 두었습니다 —
  배낭에 넣기, 바닥에서 먹이기, 정산 시 탄약 수령, 매점 구매, 튜토리얼 지급품
- `DEV_SHOW_UNLOCK=true` 면 전부 이름이 보입니다

---

## 9. 인스턴트 스킬

게이트 안에서만 유효. **나가면 전부 초기화.** 하루에 한 번, 3개 중 1택.

### 구조

```js
{k:"caf2", n:"카페인은 내친구+", br:"aspd", v:2, up:"caf",
 d:"공격 속도 +30%", f:u=>u.aspd*=1.30}
```

| 칸 | 뜻 |
|---|---|
| `k` | 저장용 열쇠. **이름을 바꿔도 세이브가 안 깨집니다** |
| `br` / `v` | 효과 갈래와 그 갈래에 쌓이는 수치 |
| `up` | 선행 스킬의 `k`. 그것을 가졌을 때만 뜨고, 고르면 **선행을 밀어내고 자리를 대신합니다**(합산 아님) |
| `req` | 갈래 수치 조건. `t` 는 지금까지 쌓인 갈래별 합계 |
| `maj` | 전공 스킬. 그 전공 대원이 파티에 있을 때만 등장 |

**상위로 갈아탄 뒤 기본 스킬은 다시 안 뜹니다** (`skillPool` 이 선행 사슬을 거슬러 제외).
전공 스킬의 `v` 는 효과 크기라서 갈래 합계에는 넣지 않습니다.

### 일반 스킬 29종

| 갈래 | 스킬 |
|---|---|
| aspd | 카페인은 내친구(+15%) → + (+30%) · 반사 신경(+8%,이동+3%) → + (+16%,+5%) |
| atkP | 새내기의 독기(+12%) → + (+24%) |
| atkF | 무기 손질(+2) → + (+5) |
| hpF | 체력이 국력(+15) → + (+30) · 지구력(+12,이동+3%) → + (+24,+5%) |
| guardF | 아프니까 청춘이다(−1~−2) → + (−4) |
| dodge | 질주(이동+8%,회피+3.5) → + (+12%,+7) · 몸놀림(+6) → + (+11) → ++ (+15) |
| heal | 줄기세포 실험체(전투 시작 +12) → + (+20) |
| rng | 길어진 팔(+2) → + (+4) |
| crit | 급소 노리기(+6%) → + (+11%) |
| mix | **인내** (체+8, 피해−1) → + (체+15, 피해−2) |
| mix | **숙련** (공·공속 +8%) → + (+18%) |

- **인내** 는 `guardF ≥ 1` **그리고** `hpF ≥ 1` 일 때 열립니다
- **숙련** 은 `atkP + atkF ≥ 2` **또는** `aspd ≥ 2` 일 때 열립니다

### 전공 스킬 11종

| 전공 | 스킬 | 효과 | 얻을 수 있나 |
|---|---|---|---|
| tkd 태권도학과 | **적수공권** | 양손이 빈 태권도학과 대원**만** 공+25%, 공속+10% | ✓ |
| nurse 간호학과 | 응급 처치 | 전투 시작마다 체력 +25 | ✓ |
| mil 군사학 | — | (아직 없음) | ✓ |
| soldier | 정조준 | 공격력 +20% | ✗ |
| police | 진압 훈련 | 받는 피해 −3 | ✗ |
| chem | 약품 조제 | 공·공속 +10% | ✗ |
| phil | 설득 | 최대 체력 +20 | ✗ |
| pe | 기초 체력 | 최대 체력 +25 | ✗ |
| cs | 조준 보정 | 사거리 +3 | ✗ |
| dance | 스텝 | 이동 +12%, 회피 +9% | ✗ |
| arch | 구조 파악 | 피해 −2, 이동 +3% | ✗ |
| lib | 약점 기록 | 공격력 +14% | ✗ |

**적수공권은 파티 전원이 아니라 조건에 맞는 대원 한 명에게만 걸립니다.**
전투 유닛이 `u.ref` 로 원래 대원을, `u.hasW`/`u.hasS` 로 양손 착용 여부를 들고 있어서 가능합니다.
같은 방식으로 "특정 대원만" 걸리는 스킬을 더 만들 수 있습니다.

**도서관을 없애면서 전공 스킬 대부분이 등장 불가가 되었습니다.**
지금 게이트에서는 사실상 일반 29종 + 적수공권/응급 처치만 돕니다.

---

## 10. 게이트 진행

- 게이트는 3개. **앞 게이트의 마지막 보스를 잡아야 다음이 열립니다** (`G.cleared` 에 id 가 쌓임)
- 하루마다 장소 2곳이 제시됨. 진입 / 둘 다 지나치기 / 게이트에서 나가기
- **1일차에는 위험도 ★ 장소만 등장** (초반 즉사 방지)
- 체력은 게이트 안에서 회복되지 않음
- 보스 예고는 이름을 밝히지 않고 분위기로만 (`bossHint()`)

### 장소 14곳

| 지형 | 위험도 | 장소 |
|---|---|---|
| — | ★ | 음식점 · 서점 · 문구점 |
| street | ★ | 포장마차 · 골목길 · 버스 정류장 |
| in | ★★ | 주점 · 원룸빌라 · 화장품가게 |
| street | ★★ | 대로 |
| under | ★★ | 지하 노래방 · 독서실 |
| under | ★★★ | 지하철역 · 경찰 본부 |

지형은 `in`(실내) / `street`(거리) / `under`(지하) 세 종류.
**경찰 본부**는 70% 확률로 총기, 평균 1.5개의 탄약 상자가 나옵니다. 골드는 박합니다.

---

## 11. 본진

### 시설과 해금 조건

| 시설 | 건물 | 조건 | 기능 |
|---|---|---|---|
| 행정실 | 본관 | 탐색 1회 | 잡화를 골드로 · 서류가방 감정 |
| 창고 겸 학생회 매점 | 본관 | 누적 골드 25 | `SHOP` = 학과 점퍼·만년필·전공책·헤어스프레이 + 탄약 · 창고 |
| 입학처 | 본관 | 탐색 2회 | 영입 20G, 새로고침 5G |
| 학생회관 | 대운동장 | 탐색 3회 | 파티 편성(최대 4명), 장비 정리 |
| 게임장 | 예체능 | 탐색 1회 | 미니게임 3종 |
| 학과사무실 | 예체능 | Lv2 | 태권도학과 |
| 응급의학 실습실 | 의학대학 | 체력 절반 이하 귀환자 발생 | 침상 1개, 탐색 1회당 10G 에 25% 회복 |
| 학과사무실 | 의학대학 | Lv2 | 간호학과 |
| 메모리얼 홀 | 의학대학 | 사망자 1명 | 사망자 스펙·기록 열람 |
| 학과사무실 | ROTC | 총기 귀환 + Lv2 | 군사학 |
| 실습동 | 이공대학 | 누적 골드 200 | **미구현** |

- 조건은 `FACILITIES` 의 `cond` / `prog` 두 줄만 고치면 됩니다
- 해금 시 안내 화면 + 확인 버튼. `onUnlock` 훅으로 이벤트 부착 가능
- **응급의학 실습실 해금 시 무료 지원자 1명 합류** (다친 인원 대체용)
- `bldOpen(b) = b.facs===null || b.facs.some(id=>facOf(id).cond(G))`
  → **`facs:[]` 인 건물은 영원히 안 열립니다.** 인문사회대학·사범대학이 그렇습니다

### 귀환 시 회복

| 상태 | 회복량 |
|---|---|
| 출동한 인원 | 최대 체력의 10% |
| 대기 인원 | 5% |
| 실습실 입원 | 40% (탐색 1회당 10G, 골드 부족 시 자동 퇴원) |

전투 카운트는 **전투를 한 번이라도 치른 탐색만** 인정.

---

## 12. 미니게임 (게임장)

**판마다 요금을 내고, 상금은 잡은 만큼.** 횟수 제한 없음.

```js
const ARCADE_FEE = {spider:10, star:10, hunt:20};
const arcadePrize = sc => sc*3;
```

| 게임 | 조작 | 규칙 |
|---|---|---|
| 거미 잡기 | 터치 | 내려오는 거미를 눌러 잡기. 3마리 놓치면 끝 |
| 별 줍기 | 끌기 + 점프 | 45초간 발판 위 별 모으기 (`STAR_TIME=45`) |
| 좀비 헌터 | 끌기 (자동 사격) | 0.11초마다 자동 발사(`GUN_COOL`), 한 방에 사살. 25마리마다 보라 좀비(10방). 3번 뚫리면 끝 |

점프 입력은 버튼·화면 탭·스페이스바 세 경로를 모두 받습니다(기기별 이벤트 차이 때문).
코요테 타임 0.12초 적용.

---

## 13. UI 규칙

작은 화면에서 그림을 키우다 보니 글자리가 좁아져서, **버튼은 평소에 숨기고 줄을 누르면 아래에 펼치는** 방식으로 통일했습니다.

| 화면 | 펼치면 나오는 것 |
|---|---|
| 장비 슬롯 | 내림 · 버림 |
| 배낭 (입는 것) | 착용 · 창고 · 버림 · 동료에게 넘기기 |
| 배낭 (먹는 것) | 먹는다 · 창고 · 버림 · 동료에게 넘기기 |
| 전리품 정리(바닥) | 대원 이름(배낭에) · 버린다 |
| 매점 판매대 / 탄약 | ○○ 골드에 산다 |
| 창고 (매점·출동 직전) | 누구에게 + 대원 버튼 |
| 행정실 감정 | 열어본다 |

- 선택 상태는 `G.invSel`(장비칸), `G.rowSel`(판매대·창고·감정), `G.sel`(전리품 바닥)
- 접혀 있을 때도 **값은 요약줄에 보입니다** (`창고 2 · 10 골드`)
- 물건이 자리를 옮기면 번호가 밀리므로 **어떤 동작이든 하고 나면 선택이 풀립니다**
- 공용 CSS 클래스: `.tap` (누를 수 있는 줄), `.open` (펼쳐진 줄), `.caret` (▾/▴), `.acts` (버튼 묶음)

### 구매 피드백

버튼을 누르면 화면을 통째로 다시 그리므로, 그 자리에서 애니메이션을 걸면 즉시 지워집니다.
**무엇을 눌렀고 얼마가 나갔는지만 적어 두었다가 다시 그린 뒤 재생**합니다 (`markTap` → `playTap`).
버튼 축소 + 노란 링 + `−15 G` 떠오름 + 상단 골드 튐.

### 알림

`toast(문구)` 는 `document.body` 에 직접 붙여서 화면을 다시 그려도 2.4초간 남습니다.
지금은 전공 불일치에만 씁니다.

### NPC 초상

`NPC_ART` 에 등록하면 대사줄과 첫 방문 화면 양쪽에 나옵니다.
현재: 행정실장(`office`), 매점 총무(`shop`). 나머지는 빈 칸.

---

## 14. 세이브

`SAVE_SLOTS=3`, `SAVE_VER=3`, 키는 `ucw.save.v3.{0..2}`.
**스킬은 배열 번호가 아니라 `k` 로 저장합니다.** 스킬을 추가·삭제해도 세이브가 안 깨집니다.
버전을 올리면 이전 판 세이브는 못 읽고, `v2` 자리는 자동으로 지웁니다.

`G` 에 들어 있는 진행 상태 중 헷갈리기 쉬운 것:

| 칸 | 뜻 |
|---|---|
| `gunSeen` | 총기 종류별로 한 번이라도 손에 넣었나 → 매점 탄약 판매 |
| `gunHome` | 총기를 들고 살아 귀환했나 → ROTC 해금 |
| `itemSeen` | 물건 도감 |
| `facesUsed` | 이미 나간 초상 |
| `announced` | 해금 안내를 이미 본 시설 |
| `instants` | 이번 게이트의 스킬 (귀환 시 초기화) |

---

## 15. 알려진 이슈 / 다음 할 일

### 구조적으로 비어 있는 것

- **인문사회대학 · 사범대학** — `facs:[]` 라 영구 잠김. 학과사무실을 붙일지 정해야 함
- **실습동** (이공대학) — 해금은 되지만 안이 비어 있음
- **전공활용 스탯** — 아무 효과도 없음
- **전공 스킬 9종** — 배정해 줄 학과사무실이 없어 등장 불가
- **군사학** — 전공 스킬 없음
- 후문 게이트(Lv5) / 강의동 게이트(Lv10) 내용물
- 6일차 이후 보스 (`BOSS_DAY` 에 줄만 추가하면 됨)
- 깨진 소주병 등 현장 무기의 내구도

### 균형이 애매한 것

- **좀비 보스**: 공속 100이라 닿으면 즉사. 이동 15로 느린 게 유일한 약점
- **Blood Eyes**: 이미 "닿으면 즉사" 상태라 공속을 더 올려도 의미 없음
- **좀비 헌터**: 조준 실력에 따른 점수 차가 거의 없음(총알이 너무 촘촘)

### 정한 방향

- MP 대신 **쿨다운제**. 전공활용 스탯이 위력과 재사용 속도를 정함
  (대안으로 검토했던 것: 과부하 게이지, 집중 게이지)
- 전투 중 플레이어 개입 없음 (완전 자동)
- 그래픽은 그림이 있으면 `drawImage`, 없으면 캔버스 코드로 그림.
  **폴백을 지울 것.** 그림 없는 몬스터가 남아 있는 동안은 반드시 필요합니다

---

## 16. 코드에서 자주 고치는 자리

| 목적 | 위치 |
|---|---|
| 개발 표시 끄기 | 최상단 `DEV_SHOW_UNLOCK` |
| 적 수치 | `ENEMIES` |
| 게이트·보스 일정·잡몹 | `GATES`, `BOSS_ADDS` · 해금 판정은 `gateOpen()` · 진행 중 게이트는 `gateOf()` |
| 아이템 | `ITEMS` (`slot`/`kind`/`req`/`frail`/`use`/`ammo`/`dodge`/`crit`) |
| 현장 장비 강화판 | `DARK_UP` (바뀌는 수치만) · 이름·id 는 `darkId()` · 쓰는 곳은 `darkTargets()`·`darkUse()` |
| 결정 등장 확률 | `CRYSTAL_RATE`, `CRYSTAL_RISK` (`rollLoot`) |
| 매점 판매 목록 | `SHOP` |
| 장소와 전리품 | `PLACES` (`junk`/`gear`/`special`/`ammo`) |
| 시설 해금 | `FACILITIES` 의 `cond`·`prog`·`msg`·`onUnlock`·`major` |
| 건물 배치 | `BUILDINGS` (`box` 는 캠퍼스 그림 위 %좌표, `facs` 가 비면 영구 잠김) |
| 시설 ↔ 화면 연결 | `FAC_SCENE` |
| 스킬 | `INSTANTS` · 등장 규칙은 `skillPool()` · 획득은 `learnSkill()` |
| 전공 | `MAJORS`, `MAJOR_LV`, `GUN_MAJORS` |
| 방 배치 | `ROOMS` (`lv` 가 점유 층) · 장소마다 `spawn:{a,e}` 로 대원·적 자리를 따로 가짐 |
| 기본 스폰 | `SPAWN_DEF` · 방에 `spawn` 이 없거나 방 밖에서 싸울 때 쓰임 |
| 길찾기 | `rBucket` · `passGrid` · `distField` · `nextStep` · `moveTo` |
| 광역 피해 | `SPLASH_RATE`, `SPLASH_PAD` |
| 적 수 공식 | `rollEncounter()` |
| 레벨 곡선 | `EXP_TABLE` |
| 회피 계수 | `DODGE_PER_SPD` |
| 미니게임 | `ARCADE_FEE`, `arcadePrize`, `GUN_COOL`, `TANK_EVERY`, `TANK_HP`, `STAR_TIME` |
| 그림 표 | `ITEM_ART` · `FOE_ART` · `FACE_ART` · `NPC_ART` · `FAC_ART` · `BLD_ART` |
| 전투 그리기 | `drawAlly` / `drawFoe` / `paint` |
